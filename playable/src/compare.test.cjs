const test=require('node:test'),assert=require('node:assert/strict'),loot=require('./balanced_loot.cjs'),{compare}=require('../compare.js');
const gear=()=>['weapon','armor','charm'].map((slot,id)=>({id,slot,baseLevel:10,quality:1,req:10,ops:[]}));
test('comparison includes attack/HP options and does not mutate equipped items',()=>{const gs=gear(),saved=JSON.stringify(gs);const c={...gs[0],id:10,ops:[{kind:'scalar',family:'attack',nums:[10]},{kind:'scalar',family:'hpFlat',nums:[100]}]};const r=compare(loot,10,gs,{weapon:3},c);assert.equal(JSON.stringify(gs),saved);assert.ok(Math.abs(r.after.atk-r.before.atk*1.1)<1e-9);assert.equal(r.after.hp-r.before.hp,100);assert.equal(r.current.id,0);assert.equal(r.locked,false);});
test('removing resistance shows a negative percentage-point difference',()=>{const gs=gear();gs[1].ops=[{kind:'resist',element:'fire',nums:[40]}];const r=compare(loot,10,gs,{}, {...gs[1],id:11,ops:[]});assert.equal(r.rows.find(x=>x.key==='resist.fire').delta,-40);});
test('future equipment remains a labeled hypothetical comparison at current level',()=>{const gs=gear(),r=compare(loot,10,gs,{}, {...gs[0],req:30,baseLevel:20});assert.equal(r.locked,true);assert.equal(r.before.stats.力,r.after.stats.力);assert.ok(r.after.atk>r.before.atk);});
